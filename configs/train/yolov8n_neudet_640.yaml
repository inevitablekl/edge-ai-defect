# Main YOLOv8n training config for NEU-DET at 640 input size.
experiment_name: yolov8n_neudet_640
dataset_yaml: data/yolo/neu_det/dataset.yaml
model: yolov8n.pt
task: detect
mode: train
imgsz: 640
epochs: 100
batch: 16
device: 0
workers: 4
patience: 30
seed: 42
optimizer: auto
experiment_root: experiments/training
