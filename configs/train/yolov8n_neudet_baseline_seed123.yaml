# Baseline multi-seed reproducibility run — seed=123.
# All effective training parameters are identical to v1 baseline except seed.
experiment_name: yolov8n_neudet_baseline_seed123
dataset_yaml: data/yolo/neu_det/dataset.yaml
model: models/pretrained/yolov8n.pt
task: detect
mode: train
imgsz: 640
epochs: 100
batch: 16
device: 0
workers: 4
patience: 30
seed: 123
deterministic: true
optimizer: auto
amp: false
mosaic: 1.0
experiment_root: experiments/training
