# Baseline multi-seed reproducibility run — seed=7.
# All parameters identical to v1 baseline except seed and deterministic.
experiment_name: yolov8n_neudet_baseline_seed7
dataset_yaml: data/yolo/neu_det/dataset.yaml
model: models/pretrained/yolov8n.pt
task: detect
mode: train
imgsz: 640
epochs: 100
batch: 16
device: 0
workers: 4
patience: 30
seed: 7
deterministic: true
optimizer: auto
amp: false
mosaic: 1.0
experiment_root: experiments/training
