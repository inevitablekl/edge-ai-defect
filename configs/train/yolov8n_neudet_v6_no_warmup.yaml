# v6: no warmup experiment. All else = v1 baseline.
experiment_name: yolov8n_neudet_v6_no_warmup
dataset_yaml: data/yolo/neu_det/dataset.yaml
model: models/pretrained/yolov8n.pt
task: detect
mode: train
imgsz: 640
epochs: 100
batch: 16
device: 0
workers: 4
patience: 30
seed: 42
deterministic: true
optimizer: auto
warmup_epochs: 0
amp: false
mosaic: 1.0
experiment_root: experiments/training
