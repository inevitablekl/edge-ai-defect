# Table data boundary

Formal table data must come from Phase 1 frozen assets. Derived data must record
the formula, source metric ID, and tolerance check. Do not rerun benchmarks or
hand-enter values without a source. Provenance validation is required before
formal table assembly; no bulk experimental data is copied in Step 5.
