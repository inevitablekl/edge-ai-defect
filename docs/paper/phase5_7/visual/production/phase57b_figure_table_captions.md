# Phase 5.7B Figure and Table Caption Authority

## Figures

- F1：**图1　V0、V2R和V3R三条受控数据路径。图中数值为完整路径E2E观测，输入复制载荷为名义值。**
- F2：**图2　V2R/V3R主机—设备输入路径。两者仅pageable/pinned暂存不同；复制、融合预处理与`enqueueV3`沿同一TensorRT CUDA stream单帧顺序执行，不表示跨帧重叠。**
- F3：**图3　三条路径的端到端性能。(a) 为5个独立进程FPS的均值±样本标准差；(b)(c) 为每条路径合并5400个延迟样本的均值、P95和P99。**
- F4：**图4　运行级分布与尾延迟。各点为独立进程级描述量，横向偏移仅用于区分，不表示运行配对。**

## Tables

- T1：**表1　三条受控路径的特征矩阵。检测器和TensorRT Engine相同；三条路径均为单帧顺序执行，无跨帧流水线。**
- T2：**表2　平台、模型与统一基准协议。**
- T3：**表3　三条路径在冻结工作负载和统一评价程序下的任务级正确性。**
- T4：**表4　相关工作的定性定位。“未报告”不等同于“否”。**
