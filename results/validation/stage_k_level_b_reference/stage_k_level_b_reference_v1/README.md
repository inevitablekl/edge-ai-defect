# Stage K K5.1 Python ORT Reference Bundle

This tracked directory freezes the metadata for `stage_k_level_b_reference_v1`.
The 16 input tensors and 16 Python ONNX Runtime raw outputs are local-only at:

`/home/ros2/edge-ai-local-evidence/stage_k/reference/stage_k_level_b_reference_v1/`

The canonical archive and its SHA256 are recorded in `archive_provenance.json`.
This is K5.1 only: Jetson ORT control, TensorRT Level B/C, boundary
classification, and the final K5 gate were not executed.
